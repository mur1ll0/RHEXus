<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rhexa.rhexus">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pkgProduto.ProdActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".pkgProduto.newProdActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".pkgPessoa.PessoaListActivity" />
        <activity android:name=".pkgPedido.PedidoActivity" />
        <activity android:name=".pkgPessoa.PessoaCadActivity" />
        <activity android:name=".pkgTitulo.TituloListActivity"/>
        <activity android:name=".pkgTitulo.TituloCadActivity"/>
    </application>

</manifest>